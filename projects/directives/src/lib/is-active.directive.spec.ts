import { IsActiveDirective } from './is-active.directive';

describe('IsActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsActiveDirective();
    expect(directive).toBeTruthy();
  });
});
